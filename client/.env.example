# Vite dev proxy target (the server's loopback UI listener)
FILESYNC_UI_URL=http://127.0.0.1:3001

# Bearer token — must match the server's FILESYNC_SHARED_SECRET.
# Vite injects this into proxied requests so the browser never sees it.
FILESYNC_SHARED_SECRET=replace-me-with-a-long-random-string
